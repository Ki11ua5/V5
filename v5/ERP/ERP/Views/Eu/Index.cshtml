@model IEnumerable<ERP.Models.equipo_unico>



<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div style="margin-top:2rem">
        <a href="@Url.Action("Create","Eu")"><i class="fas fa-plus"> Crear Nuevo</i> </a>
    </div>
    
    <table class="table table-striped border border-primary">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Almacen.Ingeniero.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.equipo_generico.marca)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.equipo_generico.modelo)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Almacen.Ingeniero.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.equipo_generico.marca)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.equipo_generico.modelo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.no_serie }) |
            @Html.ActionLink("Details", "Details", new { id = item.no_serie }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.no_serie })
        </td>
    </tr>
    }
    
    </table>
</body>
</html>
